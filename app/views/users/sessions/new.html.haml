.middle-box.text-center.loginscreen.animated.fadeInDown
  %div
    %div
      %h1.logo-name IN+
    %h3 Welcome to IN+
    %p
      Perfectly designed and precisely prepared admin theme with over 50 pages with extra new web app views.
      / Continually expanded and constantly improved Inspinia Admin Them (IN+)
    %p Login in. To see it in action.
    = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
      .form-group
        = f.email_field :email, autofocus: true, class: "form-control", placeholder: "Email"
      .form-group
        = f.password_field :password, autocomplete: "off", class: "form-control", required: true, placeholder: "Password"
      = f.submit "Iniciar Sesion", class: "btn btn-primary block full-width m-b"
      %a{:href => "#"}
        %small Olivadaste tu contraseña?
      %p.text-muted.text-center
        %small No tienes una cuenta?
      %a.btn.btn-sm.btn-white.btn-block{:href => "#{new_registration_path(resource_name)}"} Crear Cuenta
    %p.m-t
      %small UAGRM AR WEB © 2017
